# Build outputs
dist/
build/
*.deb

# Alloy binary (downloaded at build time, never committed)
build/alloy
alloy-linux-*
*.zip

# Version lock file for local overrides
ALLOY_VERSION.local

# Python / Ruby tooling (fpm uses Ruby)
.bundle/
vendor/bundle/

# Editor
.idea/
.vscode/
*.swp

# macOS
.DS_Store

# LLM agent working documents — local only, not for distribution
GUIDELINES.md
FEATURE_ROADMAP.md
SECURITY_ROADMAP.md
